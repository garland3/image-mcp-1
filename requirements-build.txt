# Build-time dependencies for model conversion
# These are ONLY needed to convert YOLO models to OpenVINO format
# Using CPU-only PyTorch to minimize image size (no CUDA dependencies)
ultralytics>=8.3.0
--extra-index-url https://download.pytorch.org/whl/cpu
torch>=2.0.0
torchvision>=0.15.0
